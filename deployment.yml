apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: seddaoudi/new_docedit:staging-aaa1fa3-11_docedit:staging-5849a10-10
        ports:
        - containerPort: 8080
