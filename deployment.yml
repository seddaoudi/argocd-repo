apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: seddaoudi/new_docedit:staging-043c3c1-9_docedit:staging-c1d3dd6-8_docedit:latest
        ports:
        - containerPort: 8080
